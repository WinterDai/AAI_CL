description: Confirm the clock uncertainty setting is correct.
requirements:
  value: N/A
  pattern_items: []
input_files:
- ${CHECKLIST_ROOT}/IP_project_folder/reports/constr.rpt
waivers:
  value: 2
  waive_items:
  - name: set_clock_uncertainty 0.01 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}]
      -to [get_clocks {PHASE_ALIGN_CLOCK}]
    reason: Pre-implementation phase - approved by architect (Ticket#CLK-2025-001)
  - name: set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}]
      -to [get_clocks {PHASE_ALIGN_CLOCK}]
    reason: Extra hold timing waived for initial implementation
