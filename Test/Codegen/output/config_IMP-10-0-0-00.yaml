# Generated by Developer Agent
# Generated at: 2026-01-28T11:42:31.958756

# === YAML Configuration for netlist/spef version check ===
requirements:
  value: 1
  pattern_items:
    - "Generated on:*2025*"
  
  # Validation configuration
  validation_mode: "any_match"  # Pass if ANY pattern matches ANY extracted item
  default_match: "contains"     # Use containment for default string matching
  regex_mode: "search"          # Use re.search for regex patterns
  
  # Field mapping (which field from Agent A output to validate)
  validate_field: "value"       # Check the 'value' field from Agent A output
  
  # Optional: filter by pattern_type before validation
  filter_by_pattern_type: null  # null means check all items; could be "generated_on_2025"

waivers:
  value: 0
  waive_items:
    - "Current stage is synthesis, SPEF check is not applicable."
  
  # Waiver configuration
  waiver_mode: "any_match"      # Waive if ANY waiver pattern matches
  waiver_match_type: "contains" # Use containment for waiver matching
  
  # Optional: check waivers against specific fields
  waiver_check_fields:
    - "matched_content"         # Check waiver patterns in matched_content
    - "value"                   # Also check in value field

# Metadata
item_id: "netlist_spef_version_check"
description: "Confirm the netlist/spef version is correct."
input_files:
  - "${CHECKLIST_ROOT}/IP_project_folder/logs/sta_post_syn.log"

# Execution hints for orchestrator
execution_config:
  require_all_patterns: false   # false = ANY pattern match passes (value=1 suggests at least one)
  stop_on_first_match: false    # Continue checking all items for comprehensive evidence
  case_sensitive: true          # fnmatch.fnmatchcase is case-sensitive