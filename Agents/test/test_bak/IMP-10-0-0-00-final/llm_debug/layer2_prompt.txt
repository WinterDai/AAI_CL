## Task: Generate Complete Specification (Layer 2)

Based on semantic intent and extraction fields, generate the complete ItemSpec.

## Semantic Intent
```json
{
  "check_target": "Verification that the netlist and SPEF file versions match expected or compatible values",
  "involved_formats": [
    {
      "name": "STA_Log",
      "role": "Contains file paths and references to netlist and SPEF files used in STA run",
      "expected_sections": [
        "read_spef",
        "read_verilog",
        "read_netlist",
        "file paths",
        "version information"
      ],
      "criticality": "required",
      "data_role": "indirect_reference"
    },
    {
      "name": "SPEF",
      "role": "Target file containing parasitic data with version information to verify",
      "expected_sections": [
        "*SPEF",
        "*DESIGN",
        "*VERSION"
      ],
      "criticality": "required",
      "data_role": "direct_source"
    },
    {
      "name": "Netlist_Verilog",
      "role": "Target file containing synthesized netlist with version comment or metadata",
      "expected_sections": [
        "module declarations",
        "version comments",
        "tool version metadata"
      ],
      "criticality": "required",
      "data_role": "direct_source"
    }
  ],
  "data_flow": "STA_Log → extract file paths → parse SPEF *VERSION field and Netlist version comments → verify against expected version patterns",
  "extraction_chain": "STA_Log → extract SPEF file path → parse SPEF *VERSION field → extract Netlist file path → parse Netlist version comment → compare both versions with expected values or compatibility rules",
  "success_criteria": "SPEF *VERSION field matches expected version format (e.g., IEEE 1481-1999) AND Netlist version comment matches expected tool version or format specification",
  "failure_criteria": "SPEF *VERSION field is missing, malformed, or incompatible OR Netlist version comment is missing or indicates incompatible version",
  "format_hints": {
    "SPEF": "IEEE 1481 standard format, *VERSION field typically in header (e.g., 'IEEE 1481-1998', 'IEEE 1481-1999')",
    "Netlist_Verilog": "Verilog format, version info typically in header comments generated by synthesis tool",
    "STA_Log": "Tool-specific log format containing read_spef and read_verilog commands with file paths"
  }
}
```

## Extraction Fields (with source_type)
```json
{}
```

## Configuration Summary
Item ID: IMP-10-0-0-00
Description: Confirm the netlist/spef version is correct.
File Count: 1
Requirements: {"value": 1, "pattern_items": ["Generated on:*2025*"]}

## Type Rules Reference
{
  "Type 1": {
    "name": "Boolean Check",
    "description": "Boolean check - PASS/FAIL only, no pattern search, no waiver",
    "needs_pattern_search": false,
    "needs_waiver_logic": false
  },
  "Type 2": {
    "name": "Value Check",
    "description": "Value check - search pattern_items, no waiver support",
    "needs_pattern_search": true,
    "needs_waiver_logic": false
  },
  "Type 3": {
    "name": "Value Check + Waiver",
    "description": "Value check with waiver - search patterns, apply waivers",
    "needs_pattern_search": true,
    "needs_waiver_logic": true
  },
  "Type 4": {
    "name": "Boolean Check + Waiver",
    "description": "Boolean check with waiver - no pattern search, apply waivers",
    "needs_pattern_search": false,
    "needs_waiver_logic": true
  }
}

## Output Requirements

Generate JSON with ALL sections below. Only include specified fields - Postprocessor handles the rest.

### 1. type_scenarios (4 types)
Each with: type_id, check_logic, output_format, example_config

### 2. check_logic
Only: parsing_method, pass_fail_logic

### 3. parse_logic
Only: file_type, parsing_strategy

### 4. type_examples (4 types)
Each with: use_case, behavior

### 5. test_scenarios (8 total: T1-T4 × PASS/FAIL)
Each with: type_id, scenario, description, input_data, expected_output

### 6. class_constants
8 templates: found_desc, missing_desc, waived_desc, found_reason, missing_reason, waived_base_reason, extra_reason, unused_waiver_reason

### 7. logic_steps
3-5 implementation steps as array

### 8. skill_reference
recommended_item (IMP-X-X-X-XX or null), selection_reason, match_confidence (high/medium/low/none)

### 9. type_execution_specs (array of 4 specs for CodeGenAgent)
Each with:
- type_id: integer (1, 2, 3, or 4)
- description: string (brief, e.g., "Boolean check for presence")
- pass_condition: string (code-like, e.g., "found_count >= 1")
- fail_condition: string (e.g., "found_count == 0")
- needs_pattern_search: boolean (true for Type 2/3)
- needs_waiver_logic: boolean (true for Type 3/4)
- output_format: string (e.g., "PASS: Found {item} | FAIL: Not found")

## source_type Handling
- "data_verified": Use exact patterns from matched_line
- "semantic_inference": Note uncertainty, suggest defensive parsing  
- "standard_format": Add IEEE/standard reference

Output the complete JSON specification.