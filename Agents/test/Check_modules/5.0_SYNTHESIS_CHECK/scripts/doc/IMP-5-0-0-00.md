# IMP-5-0-0-00 配置说明与示例

## 检查项目
**Confirm synthesis is using lib models for timing**

确认综合工具使用正确的 `.lib` 库模型进行时序分析。库模型是静态时序分析（STA）的基础，错误的库模型（如使用 NLDM 而非 CCS）可能导致时序分析不准确。

---

## 功能特性

### ✅ 自动检测四种 Checker 类型
脚本会根据 YAML 配置自动识别使用哪种类型：
- **Type 1**: 布尔检查（所有库）- 无豁免
- **Type 2**: 数值比较（禁用库匹配）- 无豁免
- **Type 3**: 数值比较 + 豁免逻辑
- **Type 4**: 布尔检查 + 豁免逻辑

### ✅ 解析 qor.rpt 报告
从 `qor.rpt` 提取 Technology libraries 信息：

**报告格式**:
```
Technology libraries:
                        tcbn03e_bwp143mh117l3p48cpd_base_elvtssgnp_0p675v_m40c_cworst_CCworst_T_ccs 110
                        tcbn03e_bwp143mh117l3p48cpd_base_lvtssgnp_0p675v_m40c_cworst_CCworst_T_ccs 111
                        tcbn03e_bwp143mh117l3p48cpd_base_ulvtssgnp_0p675v_m40c_cworst_CCworst_T_ccs 112
```

**提取字段**:
- **Library Name**: 库文件全名（含 corner 信息）
- **Line Number**: 报告中的行号

### ✅ 支持禁用库检查（Type 2/3）
- **pattern_items**: 禁用库的匹配模式（支持 glob 和 regex）
- 检测是否使用了不应该使用的库（如 NLDM 库而非 CCS 库）
- 示例：`*nldm*` 匹配所有包含 "nldm" 的库名

### ✅ Waiver 标签规则 (2025-12-02)

**当 waivers.value > 0 时（Type 3/4）**:
- 已豁免的禁用库 → INFO + `[WAIVER]`
- 未使用的豁免 → WARN + `[WAIVER]`

**当 waivers.value = 0 时（Type 1/2）**:
- 检查发现的 FAIL → INFO + `[WAIVED_AS_INFO]`（强制豁免）
- waive_items 配置项 → INFO + `[WAIVED_INFO]`（配置的豁免）
- 强制 PASS（所有失败都被豁免）

**当 waivers.value = N/A 时（Type 1/2）**:
- 正常模式，根据实际检查结果判定 PASS/FAIL

### ✅ 自定义分组描述 (Type 3)
Type 3 使用自定义错误分组描述：
- **ERROR01**: "Forbidden library found (not waived)"
- **INFO01**: "Forbidden library found but waived"
- **WARN01**: "Waiver not used"

---

## 📋 配置文件示例

### 🔹 Type 1: 布尔检查（所有库）

**用途**: 验证综合工具加载了库模型并报告库列表

#### 方案 A: 正常模式 (waivers.value = N/A)

**配置文件**: `IMP-5-0-0-00.type1.yaml`

```yaml
# Type 1: Boolean Check (Normal Mode)
# 简单的布尔检查：报告所有加载的库
# 总是 PASS（只要能解析 qor.rpt）

description: Confirm synthesis is using lib models for timing.

requirements:
  value: N/A
  pattern_items: []

input_files: 
  - C:\IP_project\reports\qor.rpt

waivers:
  value: N/A
  waive_items: []
```

**检查逻辑**:
- ✅ 找到库文件 → PASS + 报告库列表
- ✅ 未找到库文件 → PASS（空列表）

**预期输出 (PASS - 找到库文件)**:
```
PASS:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-INFO01: All libraries loaded successfully:
  Severity: Info Occurrence: 16
  - tcbn03e_bwp143mh117l3p48cpd_base_elvtssgnp_0p675v_m40c_cworst_CCworst_T_ccs
  - tcbn03e_bwp143mh117l3p48cpd_base_lvtssgnp_0p675v_m40c_cworst_CCworst_T_ccs
  - tcbn03e_bwp143mh117l3p48cpd_base_ulvtssgnp_0p675v_m40c_cworst_CCworst_T_ccs
  ...(共 16 个库)
```

#### 方案 B: 强制PASS模式 (waivers.value = 0)

**配置文件**: `IMP-5-0-0-00.type1_waiver0.yaml`

```yaml
# Type 1: Boolean Check (Forced PASS Mode)
# 强制PASS模式：所有库都转为 INFO，后缀 [WAIVED_AS_INFO]
# 用于调试或过渡期

description: Confirm synthesis is using lib models for timing.

requirements:
  value: N/A
  pattern_items: []

input_files: 
  - C:\IP_project\reports\qor.rpt

waivers:
  value: 0                          # 强制PASS
  waive_items:
    - debug_note_1
    - debug_note_2
```

**检查逻辑**:
- 所有库 → INFO + `[WAIVED_AS_INFO]`
- waive_items → INFO + `[WAIVED_INFO]`
- 强制 PASS

**预期输出 (强制PASS)**:
```
PASS:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-INFO01: Library check (forced PASS mode):
  Severity: Info Occurrence: 18
  - tcbn03e_bwp143mh117l3p48cpd_base_elvt...[WAIVED_AS_INFO]
  - tcbn03e_bwp143mh117l3p48cpd_base_lvt...[WAIVED_AS_INFO]
  - debug_note_1[WAIVED_INFO]
  - debug_note_2[WAIVED_INFO]
```

---

### 🔹 Type 2: 数值比较（禁用库检查）

**用途**: 检测是否使用了不应该使用的库（如 NLDM 库）

#### 方案 A: 正常模式 (waivers.value = N/A)

**配置文件**: `IMP-5-0-0-00.type2.yaml`

```yaml
# Type 2: Value Comparison (Normal Mode)
# 数值比较：检测禁用库模式匹配
# requirements.value 应等于期望发现的禁用库数量（通常为 0）

description: Confirm synthesis is using lib models for timing.

requirements:
  value: 0                          # 期望 0 个禁用库
  pattern_items:                    # 禁用库匹配模式
    - "*nldm*"                      # 禁用 NLDM 库
    - "*typ*"                       # 禁用 TYP corner 库

input_files: 
  - C:\IP_project\reports\qor.rpt

waivers:
  value: N/A
  waive_items: []
```

**检查逻辑**:
- 匹配 `pattern_items` 中的禁用库模式
- **找到禁用库** → FAIL（不应使用这些库）
- **未找到禁用库** → PASS（符合预期）
- **PASS**: 禁用库数量 == requirements.value
- **FAIL**: 禁用库数量 != requirements.value

**预期输出 (PASS - 未使用禁用库)**:
```
PASS:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-INFO01: Forbidden patterns check passed:
  Severity: Info Occurrence: 2
  - *nldm* (未找到匹配库)
  - *typ* (未找到匹配库)
```

**预期输出 (FAIL - 使用了禁用库)**:
```
FAIL:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-ERROR01: Forbidden libraries found:
  Severity: Fail Occurrence: 2
  - tcbn03e_bwp143mh117l3p48cpd_base_elvt_nldm (matches pattern: *nldm*)
  - tcbn03e_bwp143mh117l3p48cpd_base_typ (matches pattern: *typ*)
```

#### 方案 B: 强制PASS模式 (waivers.value = 0)

**配置文件**: `IMP-5-0-0-00.type2_waiver0.yaml`

```yaml
# Type 2: Value Comparison (Forced PASS Mode)
# 强制PASS模式：所有检查结果都转为 INFO

description: Confirm synthesis is using lib models for timing.

requirements:
  value: 0
  pattern_items:
    - "*nldm*"

input_files: 
  - C:\IP_project\reports\qor.rpt

waivers:
  value: 0                          # 强制PASS
  waive_items:
    - debug_waive_item
```

**检查逻辑**:
- 所有检查结果 → INFO + `[WAIVED_AS_INFO]`
- waive_items → INFO + `[WAIVED_INFO]`
- 强制 PASS

**预期输出 (强制PASS)**:
```
PASS:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-INFO01: Forbidden library check (forced PASS mode):
  Severity: Info Occurrence: 2
  - tcbn03e_base_elvt_nldm (禁用库但被强制豁免)[WAIVED_AS_INFO]
  - debug_waive_item[WAIVED_INFO]
```

---

### 🔹 Type 3: 数值比较 + 豁免逻辑

**用途**: 检测禁用库，同时允许豁免某些库

**配置文件**: `IMP-5-0-0-00.type3.yaml`

```yaml
# Type 3: Value Comparison with Waiver Logic
# 数值比较 + 豁免逻辑
# pattern_items: 禁用库匹配模式
# waive_items: 允许使用的禁用库（豁免列表）

description: Confirm synthesis is using lib models for timing.

requirements:
  value: 1                          # 期望值（可选）
  pattern_items:                    # 禁用库匹配模式
    - "*nldm*"
    - "*typ*"
    - "*test_lib*"

input_files: 
  - C:\IP_project\reports\qor.rpt

waivers:
  value: 1                          # 豁免数量（必须 > 0）
  waive_items:                      # 允许的禁用库
    - tcbn03e_base_test_lib
```

**检查逻辑**:
1. **匹配禁用库**：
   - 在 **waive_items** 中 → INFO + `[WAIVER]`（已豁免）
   - 不在 **waive_items** 中 → FAIL（真正的违规）

2. **waive_items** 未使用：
   - 库未找到 → WARN + `[WAIVER]`（豁免未使用）

3. **未匹配模式的库**：
   - 不匹配任何禁用模式 → INFO（正常库）

**预期输出 (FAIL - 有未豁免的禁用库)**:
```
FAIL:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-ERROR01: Forbidden library found (not waived):
  Severity: Fail Occurrence: 1
  - tcbn03e_base_elvt_nldm

IMP-5-0-0-00-INFO01: Forbidden library found but waived:
  Severity: Info Occurrence: 1
  - tcbn03e_base_test_lib
```

**Report 文件详细内容**:
```
Fail Occurrence: 1
1: Fail: tcbn03e_base_elvt_nldm. In line 110, qor.rpt: Forbidden library found (matches pattern: *nldm*)

Info Occurrence: 2
1: Info: *typ*. In line , : Forbidden pattern not found in any library (good)
2: Info: tcbn03e_base_test_lib. In line 115, qor.rpt: Forbidden library waived[WAIVER]
```

**预期输出 (PASS with WARNING - 豁免未使用)**:
```
PASS:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-WARN01: Waiver not used:
  Severity: Warn Occurrence: 1
  - tcbn03e_base_test_lib

IMP-5-0-0-00-INFO01: All forbidden patterns not found:
  Severity: Info Occurrence: 3
  - *nldm*
  - *typ*
  - *test_lib*
```

---

### 🔹 Type 4: 布尔检查 + 豁免逻辑

**用途**: 验证库加载，同时记录豁免项

**配置文件**: `IMP-5-0-0-00.type4.yaml`

```yaml
# Type 4: Boolean Check with Waiver Logic
# 布尔检查 + 豁免逻辑
# 检查库是否加载，记录豁免项

description: Confirm synthesis is using lib models for timing.

requirements:
  value: N/A                        # 布尔检查
  pattern_items: []                 # 空列表

input_files: 
  - C:\IP_project\reports\qor.rpt

waivers:
  value: 1                          # 豁免数量（必须 > 0）
  waive_items:                      # 豁免项（仅记录）
    - optional_lib_note
```

**检查逻辑**:
- 检查库是否加载
- waive_items 作为 INFO 记录（不影响 PASS/FAIL）

**预期输出 (PASS)**:
```
PASS:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-INFO01: Waived item (approved exception):
  Severity: Info Occurrence: 1
  - optional_lib_note
```

---

## 🔍 Type 自动检测逻辑

脚本会根据配置文件自动检测类型：

```python
def detect_checker_type(self) -> int:
    """
    自动检测 checker 类型
    
    Type 1: requirements.value=N/A AND waivers.value=N/A/0
    Type 2: requirements.value>0 AND pattern_items AND waivers.value=N/A/0
    Type 3: requirements.value>0 AND pattern_items AND waivers.value>0
    Type 4: requirements.value=N/A AND waivers.value>0
    """
```

| 条件 | requirements.value | pattern_items | waivers.value | 检测类型 |
|------|-------------------|---------------|---------------|----------|
| Type 1 | N/A | 空 | N/A 或 0 | Boolean (报告所有库) |
| Type 2 | > 0 | 非空 | N/A 或 0 | Value (禁用库检查) |
| Type 3 | > 0 | 非空 | > 0 | Value + Waiver (禁用库+豁免) |
| Type 4 | N/A | 空 | > 0 | Boolean + Waiver (报告+豁免) |

---

## 📝 常见问题 (FAQ)

### Q1: pattern_items 和 waive_items 的区别？
**A**: 
- **pattern_items** (仅 Type 2/3): 禁用库的匹配模式（不应使用的库）
- **waive_items** (Type 3/4): 豁免列表（允许使用的禁用库或记录项）

### Q2: Type 2 中为什么 requirements.value = 0？
**A**: Type 2 用于检测禁用库，期望值通常为 0（即不应找到任何禁用库）。如果 value=0 且实际找到禁用库，则 FAIL。

### Q3: waivers.value = 0 的用途是什么？
**A**: 强制PASS模式，用于：
- **调试阶段**: 暂时忽略所有失败，继续测试流程
- **过渡期**: 已知问题但暂时无法修复
- **分析模式**: 收集所有问题但不阻塞流程

### Q4: 如何选择使用哪种 Type？
**A**:
- **Type 1**: 最简单，仅报告加载的库列表
- **Type 2**: 检测是否使用了禁用库（如 NLDM）
- **Type 3**: 检测禁用库，但允许部分豁免
- **Type 4**: 报告库列表，同时记录豁免项

### Q5: pattern_items 支持什么格式？
**A**: 支持两种格式：
- **Glob 模式**: `*nldm*` 匹配任何包含 "nldm" 的库名
- **正则表达式**: `^tcbn03e.*nldm$` 匹配特定格式

### Q6: 如果 qor.rpt 中没有 Technology libraries 段落会怎样？
**A**: 
- **Type 1**: PASS（空库列表）
- **Type 2/3**: FAIL（无法验证禁用库）

### Q7: 如果输入文件不存在会怎样？
**A**: 脚本会抛出 `ConfigurationError` 异常并停止执行。

---

## 🛠️ 使用指南

### 1️⃣ 准备报告文件
确保综合工具生成了 `qor.rpt` 文件。

### 2️⃣ 编辑配置文件
根据需求选择合适的 Type，编辑 `IMP-5-0-0-00.yaml`：
```yaml
description: Confirm synthesis is using lib models for timing.
requirements:
  value: N/A                        # 或具体数值
  pattern_items: []                 # 或禁用库模式
input_files: 
  - /path/to/qor.rpt
waivers:
  value: N/A                        # 或 0 或具体数值
  waive_items: []                   # 或豁免列表
```

### 3️⃣ 运行检查
```bash
python IMP-5-0-0-00.py
```

### 4️⃣ 查看结果
- **日志文件**: `Check_modules/5.0_SYNTHESIS_CHECK/logs/IMP-5-0-0-00.log`
- **报告文件**: `Check_modules/5.0_SYNTHESIS_CHECK/reports/IMP-5-0-0-00.rpt`

---

## 📊 输出格式说明

### 日志文件 (.log)
**PASS 示例**:
```
PASS:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-INFO01: All libraries loaded successfully:
  Severity: Info Occurrence: 16
  - tcbn03e_bwp143mh117l3p48cpd_base_elvt...
  - tcbn03e_bwp143mh117l3p48cpd_base_lvt...
```

**FAIL 示例**:
```
FAIL:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
IMP-5-0-0-00-ERROR01: Forbidden library found (not waived):
  Severity: Fail Occurrence: 1
  - tcbn03e_base_elvt_nldm
```

### 报告文件 (.rpt)
**详细信息**:
```
FAIL:IMP-5-0-0-00:Confirm synthesis is using lib models for timing.
Fail Occurrence: 1
1: Fail: tcbn03e_base_elvt_nldm. In line 110, qor.rpt: Forbidden library found (matches pattern: *nldm*)

Info Occurrence: 15
1: Info: tcbn03e_base_elvt_ccs. In line 111, qor.rpt: Library loaded successfully
...
```

---

## 🚀 版本历史

### v1.1 (2025-12-02)
- ✅ 更新为最新框架结构
- ✅ 使用 ConfigurationError 字符串 raise
- ✅ 简化 error_groups/info_groups/warn_groups
- ✅ 统一 waiver 标签规则
- ✅ Type 3 自定义分组描述

### v1.0 (2025-11-26)
- ✅ 初始版本发布
- ✅ 支持 4 种 Checker 类型自动检测
- ✅ 解析 qor.rpt Technology libraries
- ✅ 禁用库模式匹配（Type 2/3）

---

## 📞 技术支持

如有问题或建议，请联系开发团队。

**Author**: yyin  
**Date**: 2025-12-02
