# 开发者工作流程图

**Last Updated**: December 11, 2025 (Template Library v1.1.0)

## 完整流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                    开发者收到任务分配                                  │
│  (Manager 通过 work_dispatcher.py --assign 分配)                      │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 0: 查看分配文件                                                  │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 文件位置: Work/assignments/<developer>_<module>_<time>.yaml │   │
│  │                                                              │   │
│  │ 内容示例:                                                     │   │
│  │   assignment_id: yyin_10.0_STA_DCD_CHECK_20231205_143022    │   │
│  │   developer: yyin                                           │   │
│  │   module: 10.0_STA_DCD_CHECK                                │   │
│  │   items:                                                    │   │
│  │     - item_id: IMP-10-0-0-10                                │   │
│  │       description: "Confirm check full path group..."       │   │
│  │       skeleton_generated: true                              │   │
│  │       files_created:                                        │   │
│  │         - inputs/items/IMP-10-0-0-10.yaml                   │   │
│  │         - scripts/doc/IMP-10-0-0-10_README.md               │   │
│  │         - scripts/checker/IMP-10-0-0-10.py                  │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  [提取信息]                                                           │
│  • ITEM_ID = IMP-10-0-0-10                                          │
│  • MODULE = 10.0_STA_DCD_CHECK                                      │
│  • DESCRIPTION = "Confirm check full path group timing reports"    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 1: 更新配置文件 (2分钟)                                          │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 文件: Check_modules/{MODULE}/inputs/items/{ITEM_ID}.yaml    │   │
│  │                                                              │   │
│  │ 操作:                                                         │   │
│  │ 1. 打开 YAML 文件                                             │   │
│  │ 2. 检查 input_files 列表                                      │   │
│  │ 3. 更新为实际需要的输入文件路径                                │   │
│  │                                                              │   │
│  │ 示例:                                                         │   │
│  │   input_files:                                              │   │
│  │     - reports/timing_in2out.rpt                             │   │
│  │     - reports/timing_in2reg.rpt                             │   │
│  │     - reports/timing_reg2out.rpt                            │   │
│  │     - reports/timing_reg2reg.rpt                            │   │
│  │     - reports/timing_default.rpt                            │   │
│  │     - reports/timing_cgdefault.rpt                          │   │
│  │                                                              │   │
│  │ 保持默认:                                                     │   │
│  │   requirements:                                             │   │
│  │     value: N/A                                              │   │
│  │   waivers:                                                  │   │
│  │     value: N/A                                              │   │
│  └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 2: 完善 README 文档 (10分钟)                                    │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 文件: Check_modules/{MODULE}/scripts/doc/{ITEM_ID}_README.md│   │
│  │                                                              │   │
│  │ 方法 A: 手动编辑                                              │   │
│  │   1. 删除所有 TODO 占位符                                     │   │
│  │   2. 填写 Overview (Category, Input Files, Description)     │   │
│  │   3. 填写 Check Logic (Parsing, Detection)                  │   │
│  │   4. 添加 4 种配置示例 (Type 1/2/3/4)                         │   │
│  │   5. 添加 Testing 说明                                        │   │
│  │                                                              │   │
│  │ 方法 B: 使用 AI 助手 (推荐)                                    │   │
│  │   1. 打开 DEVELOPER_TASK_PROMPTS.md                          │   │
│  │   2. 找到 "Step 2: Update README"                            │   │
│  │   3. 替换占位符:                                              │   │
│  │      {ITEM_ID} → IMP-10-0-0-10                              │   │
│  │      {MODULE} → 10.0_STA_DCD_CHECK                          │   │
│  │      {DESCRIPTION} → "Confirm check full path..."          │   │
│  │   4. 复制完整 prompt 给 AI (ChatGPT/Claude/Copilot)          │   │
│  │   5. AI 生成内容,粘贴到 README                                │   │
│  └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 2.5: 分析输入文件格式 (10分钟) - 编码前必须执行                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 目的: 理解实际输入文件格式,避免基于假设编写错误的模式匹配        │
│  │                                                              │   │
│  │ 操作步骤:                                                     │
│  │                                                              │   │
│  │ 1. 读取实际输入文件                                           │
│  │    • 从 Check_modules/{MODULE}/inputs/ 找到示例文件          │
│  │    • 查看文件内容和结构                                       │
│  │    • 记录关键字段、分隔符、格式                                │
│  │                                                              │   │
│  │ 2. 识别解析模式                                              │
│  │    • 确定文件类型: 日志? 报告? JSON? 其他?                    │
│  │    • 识别关键标识符和模式                                     │
│  │    • 示例: 日志中是 `-group <name>` 还是 `_<name>_`?         │
│  │    • 示例: 文件路径在命令后还是独立一行?                       │
│  │                                                              │   │
│  │ 3. 验证假设                                                  │
│  │    • 用真实数据测试模式匹配                                    │
│  │    • 确认提取的数据符合预期                                    │
│  │    • 处理边界情况 (空文件、缺失字段等)                         │
│  │                                                              │   │
│  │ 4. 定义输出格式                                              │
│  │    • INFO01 显示什么? (文件路径/组名/数值)                    │
│  │    • INFO02 显示什么? (如果需要)                             │
│  │    • ERROR01 显示什么?                                       │
│  │    • 豁免项如何显示? (name 和 reason 分离)                    │
│  │    • 理解 info_groups 与 details 的匹配关系                  │
│  │                                                              │   │
│  │ 常见错误:                                                     │
│  │    ✗ 基于假设编写模式,未用真实数据验证                         │
│  │    ✗ 模式匹配期望 `-group`,实际文件是 `_group_`               │
│  │    ✗ 输出显示命令文本,而不是提取的文件路径                      │
│  │    ✗ info_groups items 与 details name 不匹配                │
│  │                                                              │   │
│  │ 成功标准:                                                     │
│  │    ✓ 已查看实际输入文件内容                                    │
│  │    ✓ 已用真实数据测试解析模式                                  │
│  │    ✓ 已定义清晰的输出格式                                      │
│  │    ✓ 理解 output_formatter 的匹配逻辑                         │
│  └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 3: 实现 Python 代码 (30-60分钟)                                 │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 文件: Check_modules/{MODULE}/scripts/checker/{ITEM_ID}.py   │   │
│  │                                                              │   │
│  │ 💡 优先策略: 使用模板库 (v1.1.0 推荐)                          │   │
│  │   ✅ 位置: common/checker_templates/                          │   │
│  │   ✅ 3 个 Mixins: InputParser + WaiverHandler + OutputBuilder│   │
│  │   ✅ 代码减少: 40-70%                                          │   │
│  │   ✅ 参考: IMP-10-0-0-02 (模板重用) 和 IMP-10-0-0-10 (完整迁移)│   │
│  │                                                              │   │
│  │ 推荐继承模板:                                                  │   │
│  │   from checker_templates import (                            │   │
│  │       InputFileParserMixin,      # 7 解析模式 + normalize_command│   │
│  │       WaiverHandlerMixin,        # 8 waiver 模式 + match_waiver_entry│   │
│  │       OutputBuilderMixin         # 6 输出模式 + 自动去重(v1.1.0)│   │
│  │   )                                                          │   │
│  │                                                              │   │
│  │   class MyChecker(BaseChecker,                               │   │
│  │                   InputFileParserMixin,                      │   │
│  │                   WaiverHandlerMixin,                        │   │
│  │                   OutputBuilderMixin):                       │   │
│  │       # 一行调用完成输出构建                                    │   │
│  │       return self.build_complete_output(...)                 │   │
│  │                                                              │   │
│  │ 必须实现的 5 个方法 (如果从头实现):                             │   │
│  │                                                              │   │
│  │ 1. _parse_files(self) -> dict                               │   │
│  │    • 使用 Step 2.5 中验证过的解析模式                         │
│  │    • 读取配置的 input_files                                   │
│  │    • 解析文件内容 (基于真实格式,不要假设)                      │
│  │    • 提取完整文件路径 (如果输出需要显示)                       │
│  │    • 返回结构化数据                                           │
│  │                                                              │   │
│  │ 2. _execute_type1(self) -> CheckResult                      │   │
│  │    • 布尔/信息类检查                                          │   │
│  │    • 不比较阈值,不支持豁免                                     │   │
│  │    • 返回 PASS/FAIL                                          │   │
│  │                                                              │   │
│  │ 3. _execute_type2(self) -> CheckResult                      │   │
│  │    • 数值比较检查 (无豁免)                                     │   │
│  │    • 与 requirements.value 比较                              │   │
│  │    • **extra_items**: pass to build_complete_output()       │   │
│  │    • **Template auto-converts to WARN/INFO**                │   │
│  │    • 返回 PASS/FAIL                                          │   │
│  │                                                              │   │
│  │ 4. _execute_type3(self) -> CheckResult                      │   │
│  │    • 数值比较检查 (支持豁免)                                   │   │
│  │    • 可豁免特定违规项 (waive_items)                           │   │
│  │    • 返回 PASS/FAIL + WAIVE 标记                             │   │
│  │                                                              │   │
│  │ 5. _execute_type4(self) -> CheckResult                      │   │
│  │    • 布尔检查 (支持整体豁免)                                   │   │
│  │    • 可豁免整个检查 (waivers.value)                           │   │
│  │    • 返回 PASS/FAIL + WAIVE 标记                             │   │
│  │                                                              │   │
│  │ 方法 A: 使用模板 (强烈推荐)                                    │   │
│  │   • 继承 3 个 Mixins                                          │   │
│  │   • 实现 _parse_input_files() - 使用模板解析方法             │   │
│  │   • 实现 _execute_typeX() - 调用 build_complete_output()     │   │
│  │   • 代码量: 50-100 行 (减少 40-70%)                          │   │
│  │   • 参考: IMP-10-0-0-02.py, IMP-10-0-0-10.py                │   │
│  │                                                              │   │
│  │ 方法 B: 手动编码 (仅特殊需求)                                  │   │
│  │   • 参考 base_checker.py 框架                                │   │
│  │   • 参考其他已完成的 checker                                  │   │
│  │   • 逐个实现 5 个方法                                         │   │
│  │   • 代码量: 150-300 行                                       │   │
│  │                                                              │   │
│  │ 方法 C: 使用 AI 助手 (推荐)                                    │   │
│  │   • 使用 DEVELOPER_TASK_PROMPTS.md 中的 Step 3              │   │
│  │   • 指定使用模板: "Use checker_templates mixins"             │   │
│  │   • 替换占位符后给 AI                                         │   │
│  │   • 一次性生成完整代码                                         │   │
│  └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 4: 测试验证 (10分钟)                                            │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 4.1 准备测试数据                                              │   │
│  │     • 创建必要的输入目录                                       │   │
│  │     • 生成测试用输入文件                                       │   │
│  │     • 准备 PASS 和 FAIL 场景                                  │   │
│  │                                                              │   │
│  │ 4.2 运行回归快照工具 (推荐)                                    │   │
│  │     python common/regression_testing/create_all_snapshots.py │   │
│  │       --modules {MODULE} --checkers {ITEM_ID} --force        │   │
│  │                                                              │   │
│  │ 4.3 验证结果                                                  │   │
│  │     • 检查终端输出: Success Rate: 100.0%                      │   │
│  │     • 检查 snapshots.json: 确认 value, is_pass, info_groups   │   │
│  │     • 检查输出文件: outputs/*.yaml, reports/*.rpt             │   │
│  │                                                              │   │
│  │ 4.4 迭代测试 (修改配置)                                        │   │
│  │     • 修改 inputs/items/{ITEM_ID}.yaml 测试不同 Type          │   │
│  │     • 重新运行快照工具验证行为变化                              │   │
│  │                                                              │   │
│  │ 4.5 边缘情况测试                                              │   │
│  │     • 缺失输入文件 → 清晰错误提示                              │   │
│  │     • 空文件 → 正常处理                                       │   │
│  │     • 错误配置 → ConfigurationError                          │   │
│  │                                                              │   │
│  │ 成功标准:                                                     │   │
│  │    ✓ 快照创建成功且数据正确                                    │   │
│  │    ✓ 输出文件格式正确                                          │   │
│  │    ✓ 错误处理正常                                              │   │
│  └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Step 5: 调试和修复 (如果集成测试失败)                                 │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 常见问题和解决方案:                                            │   │
│  │                                                              │   │
│  │ 问题 1: 模式匹配失败                                          │   │
│  │   症状: 应该找到的项未找到                                     │   │
│  │   原因: 模式基于假设,与实际格式不符                            │   │
│  │   解决: 重新检查实际文件,更新匹配模式                          │   │
│  │   示例: f'-group {group}' → f'_{group}_'                    │   │
│  │                                                              │   │
│  │ 问题 2: 输出格式错误                                          │   │
│  │   症状: 显示完整命令而非文件路径                               │   │
│  │   原因: 未正确提取路径部分                                     │   │
│  │   解决: 解析行内容,提取 > 后的路径                            │   │
│  │   示例: line.split('>')[1].strip()                          │   │
│  │                                                              │   │
│  │ 问题 3: 日志文件缺少 INFO                                     │   │
│  │   症状: 报告有 details,但日志缺少 info_groups                 │   │
│  │   原因: info_groups items 与 details name 不匹配              │   │
│  │   解决: info_groups[0].items = [d.name for d in details]    │   │
│  │                                                              │   │
│  │ 问题 4: 豁免项格式错误                                         │   │
│  │   症状: 显示 "item: reason: [WAIVER]"                        │   │
│  │   原因: reason 内容放在 name 中                                │   │
│  │   解决: DetailItem.name=item, reason="{原因}[WAIVER]"        │   │
│  └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  最终检查清单                                                          │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ □ Step 2.5: 已分析实际输入文件格式                             │   │
│  │ □ Step 2.5: 已用真实数据验证解析模式                           │   │
│  │ □ Step 2.5: 已定义清晰的输出格式                               │   │
│  │ □ 配置文件 input_files 更新完成                               │   │
│  │ □ README 无 TODO, 内容完整                                    │   │
│  │ □ 5 个方法全部实现 (或使用模板)                                │   │
│  │ □ 回归快照创建成功 (Success Rate 100%)                        │   │
│  │ □ 快照数据验证通过 (value, is_pass, details)                  │   │
│  │ □ 输出文件格式正确                                            │   │
│  │ □ 边缘情况处理正常                                            │   │
│  │ □ 代码风格符合规范                                            │   │
│  └──────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  提交代码                                                              │
│  git add Check_modules/{MODULE}/                                     │
│  git commit -m "Implement {ITEM_ID}: {DESCRIPTION}"                  │
└─────────────────────────────────────────────────────────────────────┘
```

## 时间估算

| 步骤 | 任务 | 手动实现 | 使用模板 | 使用AI+模板 |
|------|------|----------|----------|-------------|
| Step 0 | 查看分配文件 | 1 分钟 | 1 分钟 | 1 分钟 |
| Step 1 | 更新配置文件 | 2 分钟 | 2 分钟 | 2 分钟 |
| Step 2 | 完善 README | 10 分钟 | 10 分钟 | 3 分钟 |
| Step 2.5 | 分析输入文件 | 10 分钟 | 10 分钟 | 10 分钟 |
| Step 3 | 实现代码 | 30-60 分钟 | **15-25 分钟** | **5-10 分钟** |
| Step 4 | 测试验证 | 10 分钟 | 10 分钟 | 5 分钟 |
| **总计** | **单个 Checker** | **~1.2 小时** | **~50 分钟** | **~25-30 分钟** |

**关键改进 (v1.1.0)**:
- 使用模板: 代码实现时间减少 50-60%
- normalize_command(): 消除重复规范化代码
- build_complete_output(): 一行调用替代 60+ 行手动构建

## 快速决策树

```
开始开发
    │
    ├─ 问题1: 需要解析什么类型的文件?
    │   ├─ 简单文本检查 (grep/exist) → _parse_files 简单实现
    │   ├─ 结构化数据 (YAML/JSON) → 使用 yaml.safe_load()
    │   ├─ 报告文件 (rpt/log) → 正则表达式解析
    │   └─ 数据库查询 → 使用已有工具类
    │
    ├─ 问题2: Type 1 判断逻辑是什么?
    │   ├─ 文件存在性检查 → if file.exists()
    │   ├─ 关键字出现 → if pattern in content
    │   ├─ 数量统计 → len(items) > 0
    │   └─ 布尔条件 → if condition
    │
    ├─ 问题3: Type 2/3 的阈值是什么?
    │   ├─ 数量比较 → count >= requirements.value
    │   ├─ 百分比比较 → ratio >= requirements.value
    │   ├─ 时间比较 → timing <= requirements.value
    │   └─ 其他指标 → metric comparison
    │
    └─ 问题4: 豁免的粒度?
        ├─ Type 3: 特定项豁免 → 匹配 waive_items 列表
        └─ Type 4: 特定项豁免 → 匹配 waive_items 列表
```

## 常见模式速查

### 模式 1: 文件存在性检查
```python
def _parse_files(self):
    required_files = ['file1.rpt', 'file2.rpt']
    found = []
    missing = []
    for f in required_files:
        if (self.input_base / f).exists():
            found.append(f)
        else:
            missing.append(f)
    return {'found': found, 'missing': missing}
```

### 模式 2: 报告文件解析
```python
def _parse_files(self):
    violations = []
    report = self.input_base / 'violations.rpt'
    with open(report) as f:
        for line in f:
            if 'ERROR' in line:
                violations.append(line.strip())
    return {'violations': violations, 'count': len(violations)}
```

### 模式 3: 数值提取
```python
def _parse_files(self):
    import re
    report = self.input_base / 'metrics.rpt'
    with open(report) as f:
        content = f.read()
    match = re.search(r'Coverage: (\d+\.?\d*)%', content)
    coverage = float(match.group(1)) if match else 0
    return {'coverage': coverage}
```

## AI 助手使用技巧

### 技巧 1: 批量替换占位符
使用编辑器的查找替换功能:
- 查找: `{ITEM_ID}` → 替换: `IMP-10-0-0-10`
- 查找: `{MODULE}` → 替换: `10.0_STA_DCD_CHECK`
- 查找: `{DESCRIPTION}` → 替换: 实际描述

### 技巧 2: 一次性生成所有步骤
使用 DEVELOPER_TASK_PROMPTS.md 底部的 "Quick Reference" 部分,
一次性给 AI 完整上下文,生成 README + 代码 + 测试脚本

### 技巧 3: 增量式开发
- 先让 AI 生成 _parse_files()
- 测试通过后,再生成 _execute_type1()
- 逐步完成,避免一次生成太多代码导致错误

### 技巧 4: 参考已有代码
在 prompt 中加入:
"请参考 IMP-10-0-0-02.py (模板重用示例) 或 IMP-10-0-0-10.py (完整迁移示例) 的实现风格，
使用 checker_templates 中的 InputFileParserMixin, WaiverHandlerMixin, OutputBuilderMixin"

## 故障排除

### 问题 1: ConfigurationError
**原因**: YAML 配置格式错误
**解决**: 
1. 检查 requirements/waivers 结构
2. 确保 value/pattern_items/waive_items 格式正确
3. 运行 `python -c "import yaml; yaml.safe_load(open('config.yaml'))"`

### 问题 2: FileNotFoundError
**原因**: 输入文件路径错误
**解决**:
1. 检查 input_files 路径相对于 inputs/ 目录
2. 确认文件确实存在
3. 检查大小写 (Linux 系统)

### 问题 3: 输出文件未生成
**原因**: BaseChecker 未正确调用
**解决**:
1. 确保 `if __name__ == "__main__"` 部分正确
2. 检查 logger 配置
3. 查看 logs/ 目录的错误日志

### 问题 4: Type 检测错误
**原因**: YAML 配置不符合类型判断逻辑
**解决**:
1. Type 1: requirements.value=N/A, waivers.value=N/A
2. Type 2: requirements.value=<number>, waivers.value=N/A
3. Type 3: requirements.value=<number>, waivers.value=<number>
4. Type 4: requirements.value=N/A, waivers.value=<number>
