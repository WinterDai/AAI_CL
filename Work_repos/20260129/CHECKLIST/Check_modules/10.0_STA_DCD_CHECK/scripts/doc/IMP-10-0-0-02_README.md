# IMP-10-0-0-02 配置说明与示例

## 检查项目
**Confirm the clock uncertainty setting is correct**

确认 SDC (Synopsys Design Constraints) 中的时钟不确定性 (clock uncertainty) 设置正确。时钟不确定性用于补偿时钟信号的抖动和偏差，是时序分析的重要参数。

**最新更新 (2025-12-11)**:
- ✅ 重构使用 `WaiverHandlerMixin` 和 `OutputBuilderMixin` 模板
- ✅ 代码量减少约28%（836→600行）
- ✅ Type 2/3 新增额外命令检测功能：自动识别不在预期列表中的命令并输出WARN
- ✅ 改进输出格式：name显示命令本身，reason显示简洁说明

---

## 功能特性

### ✅ 自动检测四种 Checker 类型
脚本会根据 YAML 配置自动识别使用哪种类型：
- **Type 1**: 布尔检查（无豁免）
- **Type 2**: 数值比较（无豁免）
- **Type 3**: 数值比较 + 豁免逻辑
- **Type 4**: 布尔检查 + 豁免逻辑

### ✅ 解析 Constraint 报告
从 `constr.rpt` 提取时钟不确定性设置：
```
set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
```

### ✅ 提取 Metadata
**文件头信息**:
```
#  Generated by:      Cadence Tempus 24.10-d063_1
#  Generated on:      Mon Dec  1 12:34:31 2025
#  Design:            phy_cmn_phase_align_digtop
```
提取：工具名称、生成日期、设计名称

### ✅ 命令规范化
- 移除多余空格
- 标准化大括号格式：`{ CLOCK }` → `{CLOCK}`
- 保留命令结构完整性

### ✅ 精确匹配与通配符支持
- **精确匹配**: 完整命令字符串比对（不区分大小写）
- **通配符匹配**: 支持 `*` 通配符
  - 示例：`"set_clock_uncertainty * -setup *PHASE_ALIGN_CLOCK*"`
  - 可匹配不同的数值和时钟名称变化

### ✅ 额外命令检测 (Type 2/3 专属 - 2025-12-11新增)
- **自动检测**: 识别文件中存在但不在 `pattern_items` 列表的命令
- **输出级别**: WARN（警告）
- **用途**: 
  - 发现配置漂移和未预期的约束变化
  - 提醒工程师review这些预期外的设置
  - 帮助维护配置一致性
- **示例**: 如果文件有3个命令但只配置了2个pattern，第3个会被标记为需要review

### ✅ Waiver标签规则 (2025-12-02更新)
根据 `waivers.value` 的不同值，使用不同的标签：

**当 waivers.value > 0 时（Type 3/4）**:
- 所有与 waive_items 相关的输出统一使用 `[WAIVER]` 后缀
- 已豁免的违规 → INFO + `[WAIVER]`
- 未使用的豁免 → WARN + `[WAIVER]`

**当 waivers.value = 0 时（Type 1/2）**:
- 检查发现的 FAIL/WARN → INFO + `[WAIVED_AS_INFO]`（实际发现的问题被强制豁免）
- waive_items 配置项 → INFO + `[WAIVED_INFO]`（配置的豁免项）
- 强制 PASS（所有失败都被豁免）

**当 waivers.value = N/A 时（Type 1/2）**:
- 正常模式，根据实际检查结果判定 PASS/FAIL

---

## 📋 配置文件示例

### 🔹 Type 1: 布尔检查

**用途**: 简单验证是否存在任何 clock uncertainty 设置

#### 方案 A: 正常模式 (waivers.value = N/A)

**配置文件**: `IMP-10-0-0-02.type1.yaml`

```yaml
# Type 1: Boolean Check (Normal Mode)
# 简单的布尔检查：验证是否存在 clock uncertainty 设置
# 无 clock uncertainty → FAIL，存在 → PASS

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: N/A
  pattern_items: []

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: N/A
  waive_items: []
```

**检查逻辑**:
- ✅ 找到任何 `set_clock_uncertainty` 命令 → PASS
- ❌ 没有找到 → FAIL

**预期输出 (PASS - 存在设置)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: Clock uncertainty settings found:
  Severity: Info Occurrence: 2
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

# Report 文件中的详细内容:
Info Occurrence: 2
1: Info: set_clock_uncertainty 0.055 -setup... In line 125, ...\constr.rpt: Command found
2: Info: set_clock_uncertainty 0.02 -hold... In line 126, ...\constr.rpt: Command found
```

**预期输出 (FAIL - 无设置)**:
```
FAIL:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-ERROR01: No clock uncertainty settings:
  Severity: Fail Occurrence: 1
  - No set_clock_uncertainty commands found in constraint report
```

#### 方案 B: 强制PASS模式 (waivers.value = 0)

**配置文件**: `IMP-10-0-0-02.type1_waiver0.yaml`

```yaml
# Type 1: Boolean Check (Forced PASS Mode)
# 强制PASS模式：所有缺失都转为 INFO，后缀 [WAIVED_AS_INFO]
# 用于调试或过渡期

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: N/A
  pattern_items: []

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 0                          # 强制PASS
  waive_items:
    - debug_item_1
    - debug_item_2
```

**检查逻辑**:
- 所有缺失 → INFO + `[WAIVED_AS_INFO]`
- waive_items → INFO + `[WAIVED_INFO]`
- 强制 PASS

**预期输出 (强制PASS - 无设置)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: Clock uncertainty status:
  Severity: Info Occurrence: 3
  - No set_clock_uncertainty commands found (缺失被强制豁免)
  - debug_item_1 (配置的豁免项)
  - debug_item_2 (配置的豁免项)

# Report 文件中的详细内容:
Info Occurrence: 3
1: Info: No clock uncertainty found. In line 0, ...\constr.rpt: No commands found[WAIVED_AS_INFO]
2: Info: debug_item_1. In line 0, N/A: Waive item[WAIVED_INFO]
3: Info: debug_item_2. In line 0, N/A: Waive item[WAIVED_INFO]
```

---

### 🔹 Type 2: 数值比较

**用途**: 验证特定的 clock uncertainty 命令是否存在

#### 方案 A: 正常模式 (waivers.value = N/A)

**配置文件**: `IMP-10-0-0-02.type2.yaml`

```yaml
# Type 2: Value Comparison (Normal Mode)
# 数值比较：期望找到特定的 clock uncertainty 命令
# requirements.value 建议等于 pattern_items 的数量（也可以是 N/A）

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: 2                          # 期望 2 个命令（建议与 pattern_items 数量一致）
  pattern_items:                    # 期望的命令（支持通配符）
    - "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
    - "set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: N/A
  waive_items: []
```

**检查逻辑**:
- 精确匹配 `pattern_items` 中的命令
- **Matched** → INFO（期望的命令）
- **Missing** → FAIL（期望但未找到）
- **Extra** → WARN（文件中存在但未在 pattern_items 中定义，需要review）
- **PASS**: 所有 pattern_items 都找到且无errors
- **FAIL**: 有 missing 命令或有errors

**新增功能 (2025-12-11)**:
- 自动检测文件中不在 pattern_items 列表的额外命令
- 额外命令输出为 **WARN**，提醒需要review这些预期外的设置
- 帮助发现配置漂移和未预期的时钟约束变化

**预期输出 (PASS - 完全匹配)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct
Value: 2 (Expected: 2)

IMP-10-0-0-02-INFO01: Expected clock uncertainty commands found:
  Severity: Info Occurrence: 2
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

# Report 文件中的详细内容:
Info Occurrence: 2
1: Info: set_clock_uncertainty 0.055 -setup... In line 14, ...\constr.rpt: clock uncertainty setting is correct and as expected
2: Info: set_clock_uncertainty 0.02 -hold... In line 13, ...\constr.rpt: clock uncertainty setting is correct and as expected
```

**预期输出 (FAIL - 缺少期望命令)**:
```
FAIL:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct
Value: 1 (Expected: 2)

IMP-10-0-0-02-INFO01: Expected clock uncertainty commands found:
  Severity: Info Occurrence: 1
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

IMP-10-0-0-02-ERROR01: Missing expected commands:
  Severity: Fail Occurrence: 1
  - set_clock_uncertainty 0.01 -hold -from [get_clocks { PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

# Report 文件中的详细内容:
Fail Occurrence: 1
1: Fail: set_clock_uncertainty 0.01 -hold... : Required clock uncertainty setting not found

Info Occurrence: 1
1: Info: set_clock_uncertainty 0.055 -setup... In line 14, ...\constr.rpt: clock uncertainty setting is correct and as expected
```

**预期输出 (PASS with WARN - 存在额外命令)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct
Value: 2 (Expected: 2)

IMP-10-0-0-02-INFO01: Expected clock uncertainty commands found:
  Severity: Info Occurrence: 2
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

IMP-10-0-0-02-WARN01: Unexpected clock uncertainty settings need review:
  Severity: Warn Occurrence: 1
  - set_clock_uncertainty 0.03 -setup -from [get_clocks {DEBUG_CLOCK}] -to [get_clocks {DEBUG_CLOCK}]

# Report 文件中的详细内容:
Warn Occurrence: 1
1: Warn: set_clock_uncertainty 0.03 -setup... In line 15, ...\constr.rpt: Unexpected clock uncertainty setting found, needs review

Info Occurrence: 2
1: Info: set_clock_uncertainty 0.055 -setup... In line 14, ...\constr.rpt: clock uncertainty setting is correct and as expected
2: Info: set_clock_uncertainty 0.02 -hold... In line 13, ...\constr.rpt: clock uncertainty setting is correct and as expected
```

#### 方案 B: 强制PASS模式 (waivers.value = 0)

**配置文件**: `IMP-10-0-0-02.type2_waiver0.yaml`

```yaml
# Type 2: Value Comparison (Forced PASS Mode)
# 强制PASS模式：所有匹配/缺失都转为 INFO
# 所有 FAIL/WARN → INFO + [WAIVED_AS_INFO]
# waive_items → INFO + [WAIVED_INFO]

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: 2                          # 可以是任意值或 N/A
  pattern_items:
    - "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
    - "set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 0                          # 强制PASS
  waive_items:
    - debug_waive_item
```

**检查逻辑**:
- 所有 matched/missing → INFO + `[WAIVED_AS_INFO]`
- waive_items → INFO + `[WAIVED_INFO]`
- 强制 PASS

**预期输出 (强制PASS - 有缺失命令)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct
Value: 1

IMP-10-0-0-02-INFO01: Clock uncertainty command check:
  Severity: Info Occurrence: 3
  - set_clock_uncertainty 0.055 -setup... (已找到)
  - set_clock_uncertainty 0.02 -hold... (缺失但被豁免)
  - debug_waive_item (配置的豁免项)

# Report 文件中的详细内容:
Info Occurrence: 3
1: Info: set_clock_uncertainty 0.055 -setup... In line 125, ...\constr.rpt: Expected command found[WAIVED_AS_INFO]
2: Info: set_clock_uncertainty 0.02 -hold... In line 0, ...\constr.rpt: Expected command not found[WAIVED_AS_INFO]
3: Info: debug_waive_item. In line 0, N/A: Waive item[WAIVED_INFO]
```

---

### 🔹 Type 3: 数值比较 + 豁免逻辑

**用途**: 验证必需的命令，同时允许豁免某些可选命令

**配置文件**: `IMP-10-0-0-02.type3.yaml`

```yaml
# Type 3: Value Comparison WITH Waiver Logic
# 数值比较 + 豁免：必需的命令 + 批准的豁免项
# requirements.value 建议等于 pattern_items 的数量（也可以是 N/A）
# waivers.value 必须 > 0，建议等于 waive_items 的数量

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: 2                          # 期望 2 个必需命令（建议与 pattern_items 数量一致）
  pattern_items:                    # 必需的命令
    - "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
    - "set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 1                          # 允许 1 个豁免（必须 > 0，建议与 waive_items 数量一致）
  waive_items:                      # 可选的命令（已批准豁免）
    - name: "set_clock_uncertainty * -from [get_clocks {DEBUG_CLOCK}] *"
      reason: "Debug clock uncertainty is optional - approved for RTL verification (Ticket#CLK-2025-0042)"
```

**检查逻辑**:
- **Expected Match** → INFO（必需的命令）
- **Waived Match** → INFO + `[WAIVER]`（豁免的可选命令）
- **Missing Expected** → FAIL（必需但未找到）
- **Extra (Unexpected)** → WARN（既不在 pattern 也不在 waive，但存在于文件中，需要review）
- **Unused Waiver** → WARN + `[WAIVER]`（配置的豁免未使用）
- **PASS**: 无 FAIL
- **FAIL**: 有 missing expected

**新增功能 (2025-12-11)**:
- 自动检测文件中不在 pattern_items 也不在 waive_items 的额外命令
- 额外命令输出为 **WARN**，提醒需要review这些预期外的设置
- 帮助发现配置漂移和未预期的时钟约束变化

**预期输出 (PASS - 全部匹配)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: Expected commands found:
  Severity: Info Occurrence: 2
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

IMP-10-0-0-02-INFO02: Commands waived:
  Severity: Info Occurrence: 1
  - set_clock_uncertainty 0.03 -from [get_clocks {DEBUG_CLOCK}] -to [get_clocks {DEBUG_CLOCK}]

# Report 文件中的详细内容:
Info Occurrence: 3
1: Info: set_clock_uncertainty 0.055 -setup... In line 14, ...\constr.rpt: clock uncertainty setting is correct and as expected
2: Info: set_clock_uncertainty 0.02 -hold... In line 13, ...\constr.rpt: clock uncertainty setting is correct and as expected
3: Info: set_clock_uncertainty 0.03 -from [get_clocks {DEBUG_CLOCK}]... In line 15, ...\constr.rpt: Required clock uncertainty not found: Debug clock uncertainty is optional - approved for RTL verification (Ticket#CLK-2025-0042)[WAIVER]
```

**预期输出 (FAIL - 缺少必需命令)**:
```
FAIL:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: Expected commands found:
  Severity: Info Occurrence: 1
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

IMP-10-0-0-02-ERROR01: Missing expected commands:
  Severity: Fail Occurrence: 1
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

# Report 文件中的详细内容:
Fail Occurrence: 1
1: Fail: set_clock_uncertainty 0.02 -hold... : Required clock uncertainty not found

Info Occurrence: 1
1: Info: set_clock_uncertainty 0.055 -setup... In line 14, ...\constr.rpt: clock uncertainty setting is correct and as expected
```

**预期输出 (PASS with WARN - 豁免未使用)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: Expected commands found:
  Severity: Info Occurrence: 2
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

IMP-10-0-0-02-WARN01: Configured waivers not used:
  Severity: Warn Occurrence: 1
  - set_clock_uncertainty * -from [get_clocks {DEBUG_CLOCK}] *

# Report 文件中的详细内容:
Warn Occurrence: 1
1: Warn: set_clock_uncertainty * -from [get_clocks {DEBUG_CLOCK}] *: Waived item not used[WAIVER]

Info Occurrence: 2
1: Info: set_clock_uncertainty 0.055 -setup... In line 14, ...\constr.rpt: clock uncertainty setting is correct and as expected
2: Info: set_clock_uncertainty 0.02 -hold... In line 13, ...\constr.rpt: clock uncertainty setting is correct and as expected
```

**预期输出 (PASS with WARN - 存在额外命令)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: Expected commands found:
  Severity: Info Occurrence: 2
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

IMP-10-0-0-02-WARN01: Unexpected clock uncertainty settings need review:
  Severity: Warn Occurrence: 1
  - set_clock_uncertainty 0.04 -setup -from [get_clocks {EXTRA_CLOCK}] -to [get_clocks {EXTRA_CLOCK}]

# Report 文件中的详细内容:
Warn Occurrence: 1
1: Warn: set_clock_uncertainty 0.04 -setup... In line 16, ...\constr.rpt: Unexpected clock uncertainty setting found, needs review

Info Occurrence: 2
1: Info: set_clock_uncertainty 0.055 -setup... In line 14, ...\constr.rpt: clock uncertainty setting is correct and as expected
2: Info: set_clock_uncertainty 0.02 -hold... In line 13, ...\constr.rpt: clock uncertainty setting is correct and as expected
```

---

### 🔹 Type 4: 布尔检查 + 豁免逻辑

**用途**: 检查命令存在性，允许豁免某些可选命令

**配置文件**: `IMP-10-0-0-02.type4.yaml`

```yaml
# Type 4: Boolean WITH Waiver Logic
# 布尔检查 + 豁免：检查是否有 clock uncertainty，允许豁免特定命令
# waivers.value 必须 > 0，建议等于 waive_items 的数量

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: N/A
  pattern_items: []

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 1                          # 允许 1 个豁免（必须 > 0，建议与 waive_items 数量一致）
  waive_items:                      # 可选的命令（已批准）
    - name: "set_clock_uncertainty * -from [get_clocks {IO_CLOCK}] *"
      reason: "IO clock uncertainty is optional - async interface (Ticket#IO-2025-0078)"
```

**检查逻辑**:
- 找到命令 → 分类为 waived 或 non-waived
- **Waived Command** → INFO + `[WAIVER]`
- **Non-Waived Command** → INFO
- **Unused Waiver** → WARN + `[WAIVER]`
- **No Commands** → INFO（无命令但可能有豁免）
- **PASS**: 总是 PASS（只要有命令或豁免）

**预期输出 (PASS - 有命令)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: Clock uncertainty commands found:
  Severity: Info Occurrence: 2
  - set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]
  - set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]

IMP-10-0-0-02-INFO02: Commands waived:
  Severity: Info Occurrence: 1
  - set_clock_uncertainty 0.03 -from [get_clocks {IO_CLOCK}] -to [get_clocks {IO_CLOCK}]

# Report 文件中的详细内容:
Info Occurrence: 3
1: Info: set_clock_uncertainty 0.055 -setup... In line 125, ...\constr.rpt: Command found
2: Info: set_clock_uncertainty 0.02 -hold... In line 126, ...\constr.rpt: Command found
3: Info: set_clock_uncertainty 0.03 -from [get_clocks {IO_CLOCK}]... In line 128, ...\constr.rpt: Command waived[WAIVER]
```

**预期输出 (PASS - 无命令但有豁免)**:
```
PASS:IMP-10-0-0-02:Confirm the clock uncertainty setting is correct

IMP-10-0-0-02-INFO01: No clock uncertainty commands found:
  Severity: Info Occurrence: 0
  (no items)

IMP-10-0-0-02-WARN01: Configured waivers not used:
  Severity: Warn Occurrence: 1
  - set_clock_uncertainty * -from [get_clocks {IO_CLOCK}] *

# Report 文件中的详细内容:
Warn Occurrence: 1
1: Warn: set_clock_uncertainty * -from [get_clocks {IO_CLOCK}] *: Waiver not used[WAIVER]

Info Occurrence: 1
1: Info: No clock uncertainty commands. In line 0, ...\constr.rpt: No commands found
```

---

## 🔍 技术细节

### 报告解析逻辑

#### Metadata 提取
从文件头部提取元数据：
```
#  Generated by:      Cadence Tempus 24.10-d063_1
#  Generated on:      Mon Dec  1 12:34:31 2025
#  Design:            phy_cmn_phase_align_digtop
```
- Tool: `Cadence Tempus 24.10-d063_1`
- Date: `Mon Dec  1 12:34:31 2025`
- Design: `phy_cmn_phase_align_digtop`

#### 命令解析
使用正则表达式提取：
```python
pattern = r'set_clock_uncertainty\s+([\d\.]+)\s+-(setup|hold)\s+-from\s+\[get_clocks\s+\{([^}]+)\}\]\s+-to\s+\[get_clocks\s+\{([^}]+)\}\]'
```
提取字段：
- `value`: 不确定性数值（如 `0.055`）
- `type`: setup 或 hold
- `from_clock`: 源时钟名称
- `to_clock`: 目标时钟名称
- `line_number`: 命令所在行号

### 命令规范化

**规范化过程**:
1. 移除大括号内的多余空格：`{ CLOCK }` → `{CLOCK}`
2. 压缩多个空格为单个空格
3. Trim 首尾空格

**示例**:
```python
raw = "set_clock_uncertainty 0.055  -setup -from [get_clocks { PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
normalized = "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
```

### 匹配策略

**精确匹配**（不区分大小写）:
```yaml
pattern_items:
  - "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
```

**通配符匹配**:
```yaml
waive_items:
  - name: "set_clock_uncertainty * -setup * PHASE_ALIGN_CLOCK *"
```
- `*` 匹配任意字符序列
- 转换为正则表达式：`.*`

**匹配逻辑**:
```python
def _match_pattern(command, patterns):
    command_normalized = normalize(command)
    for pattern in patterns:
        pattern_normalized = normalize(pattern)
        if '*' in pattern_normalized:
            # Wildcard match
            regex = pattern_normalized.replace('*', '.*')
            if re.search(regex, command_normalized, re.IGNORECASE):
                return pattern
        else:
            # Exact match (case-insensitive)
            if pattern_normalized.lower() == command_normalized.lower():
                return pattern
    return None
```

---

## ⚠️ 重要注意事项

### 配置规则

1. **Type 1/2**: 
   - `requirements.value` 可以是 `N/A`、`0` 或任意正数
   - 建议：Type 2 的 `requirements.value` 等于 `len(pattern_items)`
   
2. **Type 3/4**: 
   - `waivers.value` 必须 `> 0`（这是 Type 3/4 的定义条件）
   - 建议：`waivers.value` 等于 `len(waive_items)`
   
3. **Type 1/2 的 waiver=0 模式**:
   - 设置 `waivers.value = 0` 启用强制PASS模式
   - 所有 FAIL → INFO + `[WAIVED_AS_INFO]`
   - waive_items → INFO + `[WAIVED_INFO]`
   
4. **不一致时的行为**:
   - 脚本会发出警告但继续执行
   - 建议修复配置以保证一致性

### Waiver标签含义

**`[WAIVER]`** (Type 3/4，waivers.value > 0):
- 正常豁免模式
- 用于已批准的、有计划的豁免项
- 已豁免违规 → INFO + `[WAIVER]`
- 未使用豁免 → WARN + `[WAIVER]`

**`[WAIVED_AS_INFO]`** (Type 1/2，waivers.value = 0):
- 强制豁免模式
- 表示实际检测到的违规项被强制转为 INFO
- 用于调试模式或过渡期
- 便于识别实际存在的问题

**`[WAIVED_INFO]`** (Type 1/2，waivers.value = 0):
- 强制豁免模式
- 表示 YAML 中预配置的 waive_items
- 与实际检查结果无关，仅用于记录配置

### 命令格式要求

**支持的格式**:
```
set_clock_uncertainty <value> -setup/-hold -from [get_clocks {CLOCK}] -to [get_clocks {CLOCK}]
```

**不支持的格式**:
- 没有 `-from`/`-to` 的全局设置
- 使用 `get_ports` 而非 `get_clocks`
- 跨行的命令（需要在单行内）

### 文件路径要求

- 必须使用绝对路径
- 支持 Windows 路径格式 (`C:\Users\...`)
- 文件必须存在，否则抛出 `ConfigurationError`

---

## 📁 配置文件示例（完整版）

以下是四种类型的完整配置文件，可直接复制使用。

### Type 1 - 正常模式

**文件名**: `IMP-10-0-0-02.type1.yaml`

```yaml
# Type 1: Boolean Check (Normal Mode)
# 简单的布尔检查：验证是否存在 clock uncertainty 设置
# 无 clock uncertainty → FAIL，存在 → PASS

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: N/A
  pattern_items: []

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: N/A
  waive_items: []
```

### Type 1 - 强制PASS模式

**文件名**: `IMP-10-0-0-02.type1_waiver0.yaml`

```yaml
# Type 1: Boolean Check (Forced PASS Mode)
# 强制PASS模式：所有缺失都转为 INFO，后缀 [WAIVED_AS_INFO]
# 用于调试或过渡期

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: N/A
  pattern_items: []

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 0                          # 强制PASS
  waive_items:
    - debug_item_1
    - debug_item_2
```

### Type 2 - 正常模式

**文件名**: `IMP-10-0-0-02.type2.yaml`

```yaml
# Type 2: Value Comparison (Normal Mode)
# 数值比较：期望找到特定的 clock uncertainty 命令
# requirements.value 建议等于 pattern_items 的数量（也可以是 N/A）

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: 2                          # 期望 2 个命令（建议与 pattern_items 数量一致）
  pattern_items:                    # 期望的命令（支持通配符）
    - "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
    - "set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: N/A
  waive_items: []
```

### Type 2 - 强制PASS模式

**文件名**: `IMP-10-0-0-02.type2_waiver0.yaml`

```yaml
# Type 2: Value Comparison (Forced PASS Mode)
# 强制PASS模式：所有匹配/缺失都转为 INFO
# 所有 FAIL/WARN → INFO + [WAIVED_AS_INFO]
# waive_items → INFO + [WAIVED_INFO]

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: 2                          # 可以是任意值或 N/A
  pattern_items:
    - "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
    - "set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 0                          # 强制PASS
  waive_items:
    - debug_waive_item
```

### Type 3 - 数值比较 + 豁免

**文件名**: `IMP-10-0-0-02.type3.yaml`

```yaml
# Type 3: Value Comparison WITH Waiver Logic
# 数值比较 + 豁免：必需的命令 + 批准的豁免项
# requirements.value 建议等于 pattern_items 的数量（也可以是 N/A）
# waivers.value 必须 > 0，建议等于 waive_items 的数量

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: 2                          # 期望 2 个必需命令（建议与 pattern_items 数量一致）
  pattern_items:                    # 必需的命令
    - "set_clock_uncertainty 0.055 -setup -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"
    - "set_clock_uncertainty 0.02 -hold -from [get_clocks {PHASE_ALIGN_CLOCK}] -to [get_clocks {PHASE_ALIGN_CLOCK}]"

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 1                          # 允许 1 个豁免（必须 > 0，建议与 waive_items 数量一致）
  waive_items:                      # 可选的命令（已批准豁免）
    - name: "set_clock_uncertainty * -from [get_clocks {DEBUG_CLOCK}] *"
      reason: "Debug clock uncertainty is optional - approved for RTL verification (Ticket#CLK-2025-0042)"
```

### Type 4 - 布尔检查 + 豁免

**文件名**: `IMP-10-0-0-02.type4.yaml`

```yaml
# Type 4: Boolean WITH Waiver Logic
# 布尔检查 + 豁免：检查是否有 clock uncertainty，允许豁免特定命令
# waivers.value 必须 > 0，建议等于 waive_items 的数量

description: Confirm the clock uncertainty setting is correct.

requirements:
  value: N/A
  pattern_items: []

input_files: C:\Users\yuyin\Desktop\CHECKLIST\IP_project_folder\reports\constr.rpt

waivers:
  value: 1                          # 允许 1 个豁免（必须 > 0，建议与 waive_items 数量一致）
  waive_items:                      # 可选的命令（已批准）
    - name: "set_clock_uncertainty * -from [get_clocks {IO_CLOCK}] *"
      reason: "IO clock uncertainty is optional - async interface (Ticket#IO-2025-0078)"
```

---

## 🚀 执行示例

### 运行单个 Checker
```powershell
cd Work
python ..\Check_modules\common\check_flowtool.py `
    -root .. `
    -stage Initial `
    -check_module 10.0_STA_DCD_CHECK `
    -check_item IMP-10-0-0-02
```

### 测试不同类型
```powershell
# 测试 Type 1 (正常模式)
cp ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.type1.yaml `
   ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.yaml
python ..\Check_modules\common\check_flowtool.py -root .. -stage Initial `
    -check_module 10.0_STA_DCD_CHECK -check_item IMP-10-0-0-02

# 测试 Type 1 (强制PASS模式)
cp ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.type1_waiver0.yaml `
   ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.yaml
python ..\Check_modules\common\check_flowtool.py -root .. -stage Initial `
    -check_module 10.0_STA_DCD_CHECK -check_item IMP-10-0-0-02

# 测试 Type 2 (正常模式)
cp ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.type2.yaml `
   ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.yaml
python ..\Check_modules\common\check_flowtool.py -root .. -stage Initial `
    -check_module 10.0_STA_DCD_CHECK -check_item IMP-10-0-0-02

# 测试 Type 3 (数值比较 + 豁免)
cp ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.type3.yaml `
   ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.yaml
python ..\Check_modules\common\check_flowtool.py -root .. -stage Initial `
    -check_module 10.0_STA_DCD_CHECK -check_item IMP-10-0-0-02

# 测试 Type 4 (布尔检查 + 豁免)
cp ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.type4.yaml `
   ..\Check_modules\10.0_STA_DCD_CHECK\inputs\items\IMP-10-0-0-02.yaml
python ..\Check_modules\common\check_flowtool.py -root .. -stage Initial `
    -check_module 10.0_STA_DCD_CHECK -check_item IMP-10-0-0-02
```

### 查看结果
**日志文件**:
```
Check_modules/10.0_STA_DCD_CHECK/logs/IMP-10-0-0-02.log
```

**报告文件**:
```
Check_modules/10.0_STA_DCD_CHECK/reports/IMP-10-0-0-02.rpt
```

---

## 📚 相关文档

- **BaseChecker 框架**: `Check_modules/common/base_checker.py`
- **输出格式说明**: `Check_modules/common/output_formatter.py`
- **项目整体文档**: `Development_prompt.md`
- **框架说明**: `README.md`
- **相关检查项**: 
  - `IMP-10-0-0-00_README.md`: Netlist/SPEF 版本检查
  - `IMP-10-0-0-04_README.md`: 理想时钟网络检查

---

## 📞 支持与反馈

如有问题或建议，请联系开发团队或提交 Issue。
